# Runtime
PySide6>=6.6
pyserial>=3.5
pyqtgraph>=0.13
numpy>=1.26        # required since v0.4.0 (pyqtgraph charting); do not exclude
# sqlite3 is stdlib — used by tmstore to read truck-mcp drive-log sessions

# Dev / build
pytest>=7.0
pyinstaller>=6.0

